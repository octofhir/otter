[package]
name = "otter-extension-template"
version = "0.1.0"
edition.workspace = true
publish = false
description = "Template for embedder-defined Otter extensions: one class, one namespace, one hosted module."

[dependencies]
otter-gc = { workspace = true }
otter-macros = { workspace = true }
otter-runtime = { workspace = true }

[dev-dependencies]
tempfile = "3"
tokio = { workspace = true }
