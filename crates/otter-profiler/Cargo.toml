[package]
name = "otter-profiler"
version = "0.1.0"
edition = "2024"
description = "Profiling tools for Otter VM"
license = "MIT"

[dependencies]
parking_lot = "0.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
