[workspace]
# Generate changelog for each crate
changelog_update = true
# Create GitHub releases
git_release_enable = true
# Create git tags
git_tag_enable = true
# Use conventional commits for changelog
changelog_config = "cliff.toml"

# Release all crates together with same version
# Dependencies are automatically resolved in correct order
